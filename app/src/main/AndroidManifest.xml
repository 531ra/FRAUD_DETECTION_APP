<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcherfraud"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcherfraud_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Frauddetection"
        tools:targetApi="31">
        <activity
            android:name=".recyclerMainActivity2"
            android:exported="false" />
        <activity
            android:name=".switchMainActivity2"
            android:exported="false" />
        <activity
            android:name=".simpledetectionMainActivity2"
            android:exported="false" />
        <activity
            android:name=".introMainActivity2"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>